# R20.8 — *(операционный)* Исторический backfill 3 сезонов NHL

> **Статус:** ✅ Выполнено (операционно)
> **Выполнено:** 2026-04-26
> **Приоритет:** 🔴 High

---

## Исходное требование (из backlog R20)

- Запустить CLI backfill для окон сезонов из `conf/bookmaker/the_odds_api.yaml` → `seasons.nhl`.
- Результат в OddsStore (`data/source/nhl/odds/pinnacle_odds.parquet`, не в git).
- При необходимости — merge в `source.csv` отдельным шагом refresh/merge.

---

## Результат

- Прогон `backfill --seasons 3 --tournament nhl --store` (возможны докачки чанками при `quota_budget_per_run`).
- Store V3 заполнен; пропуски дней — по квоте/API, дозапись чанками `--from/--to`.

---

## Ограничения

- Артефакты данных и логи не коммитятся (`.gitignore`: `data/source`, `data/cache/the_odds_api/`, `logs/`).
