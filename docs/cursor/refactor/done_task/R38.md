# R38 — NHL: единый slug `nhl`

> **Статус:** ✅ Выполнено
> **Выполнено:** 2026-05-13
> **Приоритет:** 🔴 High
> **Сложность:** Medium

---

## Результат

- Канонический турнир для данных, пайплайна, promote и API — **`nhl`**.
- **`conf/tournament/nhl.yaml`**: добавлены комментарии (R26, OT, promote/materialize), блок **`train_eval_split`** (раньше в `nhl_train`).
- **`conf/tournament/nhl_train.yaml`**: deprecated — только `defaults: [nhl]` + `_self_` (старый CLI `tournament=nhl_train` даёт то же `name: nhl`).
- **`conf/source/nhl_train.yaml`**: удалён; источник только **`conf/source/nhl.yaml`**.
- **Операции:** `Makefile` (train-sweep NHL, `nhl-morning-*`), `airflow/dags/dag_nhl_morning_refresh.py` (`SF_NHL_MORNING_TOURNAMENT` default `nhl`), `scripts/run_nhl_refresh_notify.py`, `docs/source/nhl_local_operations.rst`.
- **Документация:** `docs/cursor/context/nhl_single_tournament_slug.md` (миграция `models/` и БД), обновлены `HOW_TO_ADD_NEW_TOURNAMENT.md`, `HOW_TO_ADD_NEW_MARKET.md`, `conf/split.yaml`, `reviewer-tech-debt.md`.
- **Код:** `live_odds_enrichment` по-прежнему учитывает **`nhl_train`** в строках БД для live Pinnacle; бот — комментарий о совместимости.
- **Тесты:** `test_walk_forward_integration`, `test_optuna_study_name`, `test_train_eval_split`, `test_config_validation` переведены на `nhl`.

---

## Что сделать вручную на стенде

Перенести promoted-модели **`models/nhl_train/` → `models/nhl/`** и при необходимости выполнить SQL из `nhl_single_tournament_slug.md` для строк prediction store.

---

## Ссылки

- План эпика (архив): `docs/cursor/refactor/backlog/R38.md` — обновить статус при желании или оставить как контекст исходной постановки.
