# R12.3 — Data pipeline: make dvc-repro

> **Статус:** ✅ Выполнено
> **Выполнено:** 2026-03-18
> **Приоритет:** 🟡 Medium
> **Сложность:** Medium

---

## Решение

Выполнен полный прогон Data pipeline командой `make dvc-repro` (ingest → clean → features → validate). Стадии ingest и clean пропущены (без изменений), стадия features выполнялась (multirun по всем турнирам). Падений не было. Все обнаруженные предупреждения и неожиданное поведение зафиксированы в `docs/cursor/refactor/issues-winner-full-cycle.md` с шагом «Data pipeline» и командой `make dvc-repro`.

Зафиксировано четыре записи: (1) успешный прогон для прослеживаемости; (2) FutureWarning pandas (groupby observed) в ewm_generator.py и count_generator.py; (3) FutureWarning pandera (рекомендация import pandera.pandas); (4) EWMFeatureGenerator пропуск контекстов team/h2h_team для настольного тенниса (нет pl_cteam/opp_cteam) — ожидаемое поведение, не ошибка.

## Изменённые файлы

- `docs/cursor/refactor/issues-winner-full-cycle.md` — добавлены 4 записи в секцию «Записи» (шаг Data pipeline, команда make dvc-repro).
