# R9.4 — logreg.py — SimpleImputer(strategy="mean") — оставлено как есть

> **Статус:** ✅ Выполнено
> **Выполнено:** 2026-03-20
> **Приоритет:** 🟡 Medium
> **Сложность:** Trivial

---

## Решение

Проверено: в `sports_forecast/training/models/logreg.py` (строки 158–165) числовой пайплайн содержит `SimpleImputer(strategy="mean")` и далее `StandardScaler()`, как и задумано. Изменений коду и тестам не вносилось.

**Продуктовое решение:** mean-импутация для LogReg — обычная практика; она изолирована в internal pipeline модели и не затрагивает другие алгоритмы. Warmup-фича из R9.1 уже сигнализирует об уверенности EWM; `add_indicator=True` считается избыточным.

## Изменённые файлы

- Код и тесты не менялись — только ревью/верификация указанного фрагмента `logreg.py`.
