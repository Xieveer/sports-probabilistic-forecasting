# R32. Итеративный / multi-round feature selection (автоматизация)

> **Статус:** ⏸️ Отложено — **реализация под вопросом** (нет уверенности в ROI; риск лика через повторное использование holdout).
> **Приоритет:** 🟢 Low
> **Сложность:** High

---

## Контекст

В R31 зафиксирован однопроходный сценарий FS + честные MLflow-метрики и текстовый **контракт** итераций в `docs/source/feature_selection_workflow.rst` (секция «Итеративный отбор»). Отдельного кода, CLI или DAG для цикла «отбор → метрики → снова отбор» нет.

## Проблема (если когда-нибудь вернёмся)

Автоматизировать multi-round FS без подгонки под один и тот же holdout и без нестабильного «улучшилось → ещё раунд» по шумным betting-метрикам.

## Что сделать

- [ ] **R32.1** Переоценить необходимость: сравнить прирост от single-shot `feature_selection=aggressive` vs затраты на разработку и сопровождение.
- [ ] **R32.2** Если одобрено — выбрать подход (stability selection, ограниченный forward selection, отдельный чистый validation fold), не повторять решение по тому же test, что и основная отчётность.
- [ ] **R32.3** Реализация (скрипт / флаг train) — только после R32.1–R32.2.

## Файлы (ориентиры)

- `docs/source/feature_selection_workflow.rst` — контракт R31.4
- `sports_forecast/training/trainer.py` — точка расширения при появлении `fs_round` > 1
