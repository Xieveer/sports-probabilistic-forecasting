# R9.4. logreg.py — SimpleImputer(strategy="mean") — оставлено как есть

> **Статус:** ⏭️ Пропущено
> **Приоритет:** 🟡 Medium
> **Сложность:** Trivial

---

## Решение

**Не требует изменений.**

Стандартная практика для LogReg preprocessing. Mean-импутация корректна для internal model pipeline (не влияет на данные других моделей). Warmup-фича из R9.1 уже даёт модели сигнал об уверенности EWM-значений, дублировать через `add_indicator=True` избыточно.

---

## Файлы

- `sports_forecast/training/models/logreg.py`
