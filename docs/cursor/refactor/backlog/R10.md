# R10. Исправить порядок генераторов и расхождение в количестве фичей

> **Статус:** 🟡 В работе
> **Приоритет:** 🟡 Medium
> **Сложность:** Medium

---

## Проблема

В логах DVC видно, что не все фичи генерируются. Обнаружено расхождение:
- **Ожидалось:** 191 фичей
- **Создано:** 119 фичей
- **Разница:** 72 фичи не были сгенерированы

---

## Причины

1. **Неправильный порядок генераторов:**
   - `FormFeatureGenerator` запускается ПОСЛЕ `EWMFeatureGenerator` и `CountFeatureGenerator`
   - Но EWM и Count генераторы пытаются использовать контексты, которые зависят от фичей формы:
     - `match_state` (создается FormFeatureGenerator)
     - `pl_state`, `opp_state` (создаются FormFeatureGenerator)
   - Это приводит к тому, что контексты пропускаются с warning, но фичи не генерируются

2. **Расхождение в `get_feature_names()`:**
   - Методы `get_feature_names()` возвращают список ВСЕХ фичей, которые ДОЛЖНЫ быть сгенерированы
   - Но если контексты пропускаются из-за отсутствующих колонок, фактическое количество фичей меньше
   - Это приводит к расхождению между ожидаемым (191) и фактическим (119) количеством
   - В `pipeline.py` есть warning о расхождении, но оно не очень заметно

3. **Проблема с soft-fail контекстами:**
   - Генераторы пропускают контексты с warning (например, `team` для table tennis)
   - Но `get_feature_names()` все равно возвращает полный список фичей
   - Это создает ложное ожидание количества фичей

---

## Что сделать

- [x] **R10.1** ✅ Изменить порядок генераторов: `form` должен быть ПЕРЕД `ewm` и `count`
  - Вариант A: Изменить порядок в конфигах (`conf/features/advanced.yaml`, `conf/features/basic.yaml`)
  - Вариант B: Добавить механизм зависимостей между генераторами (более сложно, но более гибко)
  - Рекомендуется: Вариант A (проще и понятнее)
  - **Выполнено:** Изменен порядок в defaults: time → form → rolling (form теперь перед rolling)

- [x] **R10.2** ✅ Исправить `get_feature_names()` чтобы возвращать только те фичи, которые реально будут сгенерированы
  - Вариант C: Разделить на `get_expected_feature_names()` и `get_actual_feature_names(df)`. `get_feature_names()` — алиас.
  - В pipeline используется `get_prefixed_actual_feature_names(result_df)` для итоговой статистики; warning о расхождении убран.
  - **Выполнено:** 2026-03-18, см. `docs/cursor/refactor/done_task/R10.2.md`

- [ ] **R10.3** Улучшить логирование пропущенных контекстов
  - Добавить итоговую сводку в конце генерации: сколько контекстов пропущено, сколько фичей не создано
  - Поднять уровень логирования с WARNING до ERROR для критичных пропусков (например, `match_state` когда form включен)
  - Добавить проверку зависимостей между генераторами в `pipeline.py`

- [ ] **R10.4** Добавить валидацию порядка генераторов
  - В `FeaturePipeline._init_generators()` проверять, что генераторы, создающие контекстные колонки, идут перед генераторами, которые их используют
  - Или добавить явные зависимости в конфигах (например, `depends_on: ["form"]`)

- [ ] **R10.5** Исправить подсчет фичей в `pipeline.py`
  - Использовать `get_actual_feature_names(df)` вместо `get_feature_names()` для итоговой статистики
  - Убрать warning о расхождении, если используется динамический подсчет

- [ ] **R10.6** Исследовать проблему с фичами `h2h_match_num` (>50% null)
  - Фичи `f_h2h_match_num_diff_ewm_5`, `f_h2h_match_num_diff_ewm_25`, `f_h2h_match_num_diff_ewm_100`,
    `f_h2h_match_num_total_ewm_5`, `f_h2h_match_num_total_ewm_25` имеют >50% null значений
  - Возможные причины:
    - Мало H2H встреч между парами игроков с одинаковым `tour_match_num`
    - Проблема с логикой группировки в EWM генераторе
    - Недостаточно данных для некоторых пар игроков
  - Решения:
    - Проверить распределение `tour_match_num` в данных
    - Добавить min_periods проверку для H2H контекстов
    - Рассмотреть удаление этих фичей или изменение логики генерации

---

## Прогресс после R10.1

После изменения порядка генераторов (form перед rolling):
- ✅ Контексты `match_state`, `form` (pl_state) больше НЕ пропускаются
- ✅ Количество фичей увеличилось: было 119, стало 167 (из 191 ожидаемых)
- ⚠️ Остается расхождение: 24 фичи (191 - 167 = 24)
- ⚠️ Пропускаются только контексты `team` и `h2h_team` из-за отсутствия `pl_cteam`/`opp_cteam` (ожидаемо для table tennis, soft-fail)

**Подсчет пропущенных фичей из-за team контекстов:**
- EWM diff: `team` (3 spans × 2 players + 3 spans × 1 diff) + `h2h_team` (3 spans × 1) = 9 + 3 = 12 фичей
- EWM total: `team` (3 spans × 2 players) + `h2h_team` (3 spans × 1) = 6 + 3 = 9 фичей
- Count: `team` (2 players) + `h2h_team` (1) = 2 + 1 = 3 фичи
- **Итого: 12 + 9 + 3 = 24 фичи** (соответствует расхождению)

**Новые проблемы из логов:**

1. **Фичи с >50% null:**
   - `f_h2h_match_num_diff_ewm_5`, `f_h2h_match_num_diff_ewm_25`, `f_h2h_match_num_diff_ewm_100`
   - `f_h2h_match_num_total_ewm_5`, `f_h2h_match_num_total_ewm_25`
   - Это может указывать на проблему с данными или логику генерации H2H фичей по `tour_match_num`

2. **Расхождение все еще есть:**
   - `get_feature_names()` возвращает полный список (191), включая фичи, которые не будут созданы из-за отсутствующих колонок
   - Это создает ложное ожидание и требует реализации R10.2

**Примеры из новых логов:**

```
[WARNING] - EWMFeatureGenerator: контекст 'team' (span=5) пропущен, отсутствуют колонки: ['pl_cteam']
[WARNING] - EWMFeatureGenerator: контекст 'h2h_team' (span=5) пропущен, отсутствуют колонки: ['pl_cteam', 'opp_cteam']
[WARNING] - CountFeatureGenerator: контекст 'team' пропущен, отсутствуют колонки: ['pl_cteam']
[WARNING] - Расхождение: ожидалось 191 фичей, создано 167
[WARNING] - ⚠ Фичи с >50% null (6): ['f_h2h_match_num_diff_ewm_5', 'f_h2h_match_num_diff_ewm_25', ...]
```

---

## Файлы

- `conf/features/advanced.yaml` — порядок генераторов
- `conf/features/basic.yaml` — порядок генераторов
- `sports_forecast/features/pipeline.py` — порядок выполнения, подсчет фичей
- `sports_forecast/features/generators/ewm_generator.py` — `get_feature_names()`, пропуск контекстов
- `sports_forecast/features/generators/count_generator.py` — `get_feature_names()`, пропуск контекстов
- `sports_forecast/features/generators/form_generator.py` — создает `pl_state`, `opp_state`, `match_state`
