# TASK-004-5 — Восстановить зелёный mypy baseline для релиза

> **Статус:** done
> **Владелец:** implementer
> **Эпик:** [EPIC-004](../EPIC-004-nhl-release-readiness.md)
> **Связанный отчёт:** [TASK-004-4](../../changes/done/TASK-004-4-nhl-preproduction-validation.md)

## Результат и границы

Сделать полный gate ``uv run pre-commit run mypy --all-files`` зелёным без
ослабления конфигурации mypy и без игнорирования ошибок. Задача ограничена
исправлением типизации в уже диагностированных 11 файлах; не меняет runtime
поведение NHL, Odds API, версионирование, Docker и production-инфраструктуру.

## Критерии приёмки

- [ ] Полный mypy проходит без ошибок.
- [ ] Для каждого исправленного поведения есть подходящий существующий или
  добавленный тест; бизнес-логика не изменена неявно.
- [ ] `make lint` и затронутые unit-тесты проходят.
- [ ] Отчёт выполнения содержит фактически выполненные команды и остаточные риски.

## Исходное доказательство

На 2026-08-09 ``uv run pre-commit run mypy --all-files`` завершился с 22
ошибками в 11 файлах. Ошибки расположены в ``utils/metrics``, ``monitoring``,
``training``, ``data/clean`` и нескольких tests; они не относятся к файлам
TASK-004-1--004-4.

## Проверка

1. Воспроизвести полный mypy до начала правок.
2. Исправлять по минимальным тематическим группам с targeted tests.
3. Запустить полный mypy, `make lint` и релевантные unit-тесты.

## План реализации

1. `utils/metrics` и `monitoring/*`: минимально уточнить NumPy-аннотации и
   выражения; проверка — targeted mypy и monitoring/metrics tests.
2. `training/*`: исправить границы ndarray и возвращаемые типы без изменения
   алгоритмов; проверка — targeted mypy и training tests.
3. `data/clean` и test-аннотации: устранить оставшиеся несовпадения API pandas/
   NumPy; проверка — targeted mypy и затронутые tests.
4. Выполнить полный mypy, lint и unit-suite; задокументировать результаты.
