#### `ingest.py`
1. Модуль не использует Hydra
2. В описании модуля нет информации о том, что он создает данные с коэффициентами букмекера
3. Зачем хардкодные функции по разделениям турнира: get_uel_subtournament, split_lp_eu_tournament, split_uel_tournament. Почемы бы логику работы с сырьем не вынести в конфигурации и сделать одну фунцию сплита, а правила смотеть в конфигах?
4. Функция parse_odds_from_dict содержит хардкод. Внутри фунции маппинги, которые могут просто упасть при смене источника. Логика должна быть вынесена
строка 393 и ниже: Точно ли обработка построчно датафрейма эффективнее чем применение функции через apply?
В process_tournament хардкод по выбору турнира. Архитектурно недопустимо такое, иначе внутри функции под каждый турнир будем добавлять?
#### `clean.py`
1. Импорт внутри функции run()
2. В фунции process_tournament пункты 5 и 5,5 какой то хардкод под один конкретный турнир. Недопустимо
3. load_tournament_config содержит OmegaConf.load(). Разве мы не должны были делать как то по другому?
4. _apply_column_mapping принимает на вход mapping: dict[str, str], а _apply_dtype_conversion принимает на вход dtype_config: DictConfig. Разве нам не нужно определиться либо словари либо конфиги на входе?
