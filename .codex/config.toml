# Проектные настройки применяются только после доверия репозиторию.
model = "gpt-6-sol"

[agents]
enabled = true
default_subagent_model = "gpt-6-sol"
default_subagent_reasoning_effort = "medium"
max_concurrent_threads_per_session = 3
